# دليل رفع الموقع على استضافة مجانية (Vercel) 🚀

بما أن مشروعك يستخدم **Next.js**، فإن أفضل وأسهل خيار مجاني هو **Vercel** (الشركة المطورة لـ Next.js).
ولكن، بما أننا نستخدم قاعدة بيانات **SQLite** (ملف محلي)، فهي لا تعمل بشكل جيد على الاستضافات السحابية لأن البيانات ستضيع عند كل تحديث للموقع.

لذلك، سنقوم بالخطوات التالية:
1.  تحويل قاعدة البيانات إلى **PostgreSQL** (مجانية عبر Vercel أو Neon).
2.  رفع الكود إلى **GitHub**.
3.  ربط المشروع بـ **Vercel**.

---

## الخطوة 1: تجهيز المشروع لـ PostgreSQL

1.  قم بتثبيت مكتبة `pg` (للاتصال بـ Postgres):
    ```bash
    npm install pg
    ```

2.  في ملف `prisma/schema.prisma`، سنحتاج لتغيير `provider` من `sqlite` إلى `postgresql`. سأقوم بذلك لك في الخطوة القادمة.

---

## الخطوة 2: رفع الكود على GitHub

إذا لم يكن الكود مرفوعاً، يجب عليك إنشاء مستودع (Repository) جديد على GitHub ورفع الملفات:

```bash
git init
git add .
git commit -m "Initial commit"
# استبدل الرابط أدناه برابط المستودع الخاص بك
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git branch -M main
git push -u origin main
```

---

## الخطوة 3: الرفع على Vercel

1.  اذهب إلى [vercel.com](https://vercel.com) وسجل حساباً (يفضل باستخدام GitHub).
2.  اضغط على **"Add New..."** ثم **"Project"**.
3.  اختر المستودع الذي رفعته للتو ثم اضغط **Import**.
4.  في إعدادات المشروع (Project Settings)، اذهب إلى قسم **Storage** وأضف قاعدة بيانات **Postgres** (Vercel توفر واحدة مجانية).
5.  سيقوم Vercel تلقائياً بملء متغيرات البيئة (`POSTGRES_URL` وغيرها).
6.  اضغط **Deploy**.

---

## الخطوة 4: تشغيل قاعدة البيانات

بعد الرفع، قد تحتاج لتشغيل هذا الأمر في Vercel (أو محلياً إذا ربطت قاعدة البيانات السحابية):
```bash
npx prisma db push
```
هذا سينشئ الجداول في قاعدة البيانات السحابية الجديدة.

---

## هل تريد مني تحويل الإعدادات الآن إلى PostgreSQL؟
هذا سيتطلب منك الحصول على رابط قاعدة بيانات (Connection String) إذا كنت تريد تجربة التشغيل محلياً، أو يمكننا إبقاء SQLite محلياً واستخدام Postgres فقط عند الرفع (يتطلب تعديلات في ملف Schema).

**الخيار الأسهل حالياً:** سأقوم بتهيئة ملف `schema.prisma` ليكون جاهزاً للعمل مع PostgreSQL عند توفير رابط في متغيرات البيئة.
