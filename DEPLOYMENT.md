# 🚀 دليل رفع الموقع على استضافة Vercel المجانية

بما أنك تستخدم بالفعل قاعدة بيانات **PostgreSQL** سحابية (عبر Prisma Data Proxy)، فإن عملية الرفع ستكون سهلة جداً!

## الخطوات المختصرة:

### 1. رفع الكود على GitHub
إذا لم تقم بذلك بعد، ارفع المشروع على مستودع جديد في GitHub:

```bash
git init
git add .
git commit -m "Ready for deploy"
# استبدل الرابط أدناه برابط المستودع الخاص بك
git remote add origin https://github.com/USERNAME/REPO_NAME.git
git branch -M main
git push -u origin main
```

### 2. الربط مع Vercel
1. اذهب إلى [vercel.com](https://vercel.com) وسجل الدخول بحساب GitHub.
2. اضغط **Add New Project**.
3. اختر المستودع الذي رفعته (Import).

### 3. إعداد متغيرات البيئة (Environment Variables)
**هذه الخطوة مهمة جداً!** في صفحة الإعدادات قبل الضغط على Deploy، افتح قسم **Environment Variables** وأضف القيم التالية (نسخاً من ملف `.env` لديك):

| الاسم (Key) | القيمة (Value) |
|---|---|
| `DATABASE_URL` | انسخ الرابط الطويل من ملف `.env` |
| `AUTH_SECRET` | انسخ الكود السري من ملف `.env` |
| `NEXT_PUBLIC_VAPID_PUBLIC_KEY` | انسخ المفتاح العام للإشعارات |
| `VAPID_PRIVATE_KEY` | انسخ المفتاح الخاص للإشعارات |

### 4. إطلاق الموقع (Deploy)
اضغط على زر **Deploy**.
سيقوم Vercel ببناء الموقع وتشغيله خلال دقيقة أو دقيقتين.

---

## 💡 ملاحظات هامة:
- **تحديث قاعدة البيانات:** بما أنك تستخدم نفس قاعدة البيانات للتطوير والإنتاج، فإن أي تغيير في `schema.prisma` يتطلب تشغيل `npx prisma db push` محلياً لتحديث الهيكلية في السيرفر.
- **عنوان URL:** بعد الرفع، سيعطيك Vercel رابطاً مجانياً (مثلاً `yemen-app.vercel.app`).
- **Google Auth:** إذا كنت تستخدم تسجيل دخول Google، ستحتاج لإضافة رابط Vercel الجديد إلى إعدادات Google Console (في Authorized Redirect URIs).
